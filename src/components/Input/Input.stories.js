import React from 'react';
import Input from 'components/Input/SearchInput';

export default {
  title: 'Input',
  component: Input,
};

export const Search = () => (
  <Input placeholder="search" />
);
